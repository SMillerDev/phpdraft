<?php
/**
 * This file contains the default template
 *
 * @package PHPDraft\HTML
 * @author  Sean Molenaar<sean@seanmolenaar.eu>
 */
use PHPDraft\Out\Minifier;
?><!DOCTYPE html>
<html lang="en">
<head>
    <title><?= $this->base_data['TITLE']; ?></title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.13.0/css/all.css" integrity="sha384-Bfad6CLCknfcloXFOyFnlgtENryhrpZCe29RTifKEixXQZ38WheV+i/6YWSzkz3V" crossorigin="anonymous">
    <?php foreach ($this->css as $style): ?><link rel="stylesheet" href="<?= $style ?>"><?php endforeach; ?>
    <style><?= Minifier::minify_css(file_get_contents($this->find_include_file($this->template, 'css'), true));?></style>
</head>
<body>
<div class="container-fluid">
    <div class="media">
        <div class="media-body">
            <h1 class="media-heading"><?= $this->base_data['TITLE']; ?>
                <?php if (isset($this->base_data['ALT_HOST'])): ?>
                    <select class="form-control">
                        <option selected><?= $this->base_data['HOST']; ?></option>
                        <?php foreach (explode(',', $this->base_data['ALT_HOST']) as $host): ?>
                            <option><?= $host; ?></option>
                        <?php endforeach; ?>
                    </select>
                <?php else: ?>
                    <small><?= $this->base_data['HOST']; ?></small>
                <?php endif; ?>
            </h1>
            <?php if (key_exists('DESC', $this->base_data)):?>
            <p class="lead"><?= $this->base_data['DESC']; ?></p>
            <?php endif;?>
        </div>
        <?php if (!is_null($this->image)): ?>
            <div class="media-right">
                <a href="#">
                    <img class="media-object" src="<?= $this->image ?>" alt="Image">
                </a>
            </div>
        <?php endif; ?>
    </div>
    <div class="row">
        <nav class="navbar col-md-2 method-nav sticky-top" id="navbar" aria-label="Categories">
            <?php foreach ($this->categories as &$category):?>
                <?php if ($category->children !== []): ?>
                    <nav id="navbar-<?= $category->title; ?>" class="navbar navbar-light category" aria-label="Category <?= $category->title; ?>">
                        <a class="navbar-brand" href="#<?= $category->get_href(); ?>"><?= $category->title; ?></a>
                        <?php foreach ($category->children as $resource): ?>
                            <nav class="nav nav-pills flex-column resource" aria-label="Resource <?= $category->title . '/' . $resource->title; ?>">
                                <a class="nav-link" href="#<?= str_replace('-', '/', $resource->get_href()); ?>"><?= $resource->title; ?></a>
                                <?php foreach ($resource->children as $transition): ?>
                                    <nav class="nav nav-pills flex-column transition" aria-label="Transition <?= $category->title . '/' . $resource->title . '/' . $transition->title; ?>">
                                        <a class="nav-link" href="#<?= str_replace('-', '/', $transition->get_href()); ?>">
                                            <?= $transition->title; ?>
                                            <span class="float-right align-baseline <?= $this->get_method_icon($transition->get_method()); ?>"></span>
                                        </a>
                                    </nav>
                                <?php endforeach; ?>
                            </nav>
                        <?php endforeach; ?>
                    </nav>
                <?php endif; ?>
            <?php endforeach; ?>
            <?php if ($this->base_structures !== []): ?>
                <nav id="nav-datastructures" class="navbar navbar-light structures" aria-label="Data structures">
                    <a class="navbar-brand" href="#<?= $category->get_href(); ?>">Data Structures</a>
                    <?php foreach ($this->base_structures as $key => $structure): ?>
                        <nav class="nav nav-pills flex-column structure" aria-label="Structure <?= $key?>">
                            <a class="nav-link" href="#object-<?= $this->strip_link_spaces($key); ?>"><?= $key; ?></a>
                        </nav>
                    <?php endforeach; ?>
                </nav>
            <?php endif; ?>
            <a class="main-url nav-link"><?= $this->base_data['HOST']; ?></a>
        </nav>
        <div class="col-md-10 main-content">
            <?php foreach ($this->categories as &$category): ?>
                <?php if (!is_null($category->title)): ?>
                    <h2>
                        <a id="<?= $category->get_href(); ?>" data-anchor-id="<?= $category->get_href(); ?>"><?= $category->title; ?></a>
                    </h2>
                <?php endif; ?>
                <?php if (!is_null($category->description)): ?>
                    <p><?= $category->description; ?></p>
                <?php endif; ?>
                <?php foreach ($category->children as $resource): ?>
                    <h3>
                        <a class="resource-title"
                           id="<?= str_replace('-', '/', $resource->get_href()); ?>"
                           data-anchor-id="<?= str_replace('-', '/', $resource->get_href()); ?>">
                            <?= $resource->title; ?>
                        </a>
                        <small><?= $resource->href; ?></small>
                    </h3>
                    <?php if (!is_null($resource->description)): ?>
                        <p><?= $resource->description; ?></p>
                    <?php endif; ?>
                    <?php if ($resource->url_variables !== null): ?>
                        <h5>URI Parameters</h5>
                        <div class="row">
                            <?= $resource->url_variables; ?>
                        </div>
                    <?php endif; ?>
                    <?php foreach ($resource->children as $transition): ?>
                        <div
                            class="card <?= $transition->get_method(); ?>">
                            <div class="card-header">
                                <h5 class="card-title">
                                    <var><?= $transition->get_method(); ?></var>
                                    <code><?= $transition->href; ?></code>
                                    <a class="float-right transition-title"
                                       id="<?= str_replace('-', '/', $transition->get_href()); ?>"
                                       data-anchor-id="<?= str_replace('-', '/', $transition->get_href()); ?>">
                                        <?= $transition->title; ?>
                                    </a>
                                </h5>
                            </div>
                            <div class="card-body">
                                <p class="lead"><?= $transition->description; ?></p>
                                <?php if ($transition->requests !== []): ?>
                                    <?php foreach ($transition->requests as $key => $request): ?>
                                        <div class="card">
                                            <div class="card-header">
                                                <h6 class="request card-title"
                                                    data-toggle="collapse"
                                                    data-target="#request-coll-<?= $request->get_id(); ?>">Request
                                                    <?php if (!is_null($request->title)): ?>
                                                        <var><?= $request->title;?></var>
                                                    <?php endif;?>
                                                    <span class="fas indicator fa-angle-down float-right"></span>
                                                </h6>
                                            </div>

                                            <div
                                                class="collapse <?php if ($key < 1): ?>show<?php endif; ?> request-card card-body"
                                                id="request-coll-<?= $request->get_id(); ?>"
                                                data-anchor-id="request-coll-<?= $request->get_id(); ?>">
                                                <?= $request->description; ?>
                                                <a class="btn btn-default curl"
                                                   role="button"
                                                   title="cURL command"
                                                   tabindex="0"
                                                   data-placement="left"
                                                   data-toggle="popover"
                                                   data-html="true"
                                                   data-content="<textarea rows='8' cols='75'><?= $transition->get_curl_command($this->base_data['HOST']); ?></textarea>">
                                                    <span class="fas fa-copy"></span>
                                                </a>
                                                <?php if ($transition->url_variables !== []): ?>
                                                    <h5>Example URI</h5>
                                                    <span class="base-url"><?= $this->base_data['HOST']; ?></span><em><?= $transition->build_url(); ?></em>
                                                <?php endif; ?>
                                                <?php if ($request->headers !== []): ?>
                                                    <h5>Headers</h5>
                                                    <ul class="headers list-unstyled">
                                                        <?php foreach ($request->headers as $name => $value): ?>
                                                            <li>
                                                                <code><span class="attr"><?= $name; ?></span>: <span
                                                                        class="value"><?= $value; ?></span>
                                                                </code>
                                                            </li>
                                                        <?php endforeach; ?>
                                                    </ul>
                                                <?php endif; ?>
                                                <?php if ($request->body !== []): ?>
                                                    <h5>Body</h5>
                                                    <?php foreach ($request->body as $value): ?>
                                                        <?php if (is_string($value)): ?>
                                                            <code class="request-body"><?= $value; ?></code>
                                                        <?php else: ?>
                                                            <?php $type =
                                                                (isset($request->headers['Content-Type'])) ? $request->headers['Content-Type'] : null; ?>
                                                            <?= $value->print_request($type); ?>
                                                            <?= $value ?>
                                                        <?php endif; ?>
                                                    <?php endforeach; ?>
                                                <?php endif; ?>
                                                <?php if ($request->struct !== []): ?>
                                                    <h5>Structure</h5>
                                                    <div class="row">
                                                        <?= $request->struct ?>
                                                    </div>
                                                <?php endif; ?>

                                                <?php if ($transition->url_variables !== null): ?>
                                                    <h5>URI Parameters</h5>
                                                    <div class="row">
                                                        <?= $transition->url_variables; ?>
                                                    </div>
                                                <?php endif; ?>

                                                <?php if ($transition->data_variables !== null): ?>
                                                    <h5>Request object</h5>
                                                    <div class="row">
                                                        <?= $transition->data_variables ?>
                                                    </div>
                                                <?php endif; ?>
                                            </div>
                                        </div>
                                    <?php endforeach; ?>
                                <?php endif; ?>
                                <?php if (isset($transition->responses)): ?>
                                    <?php foreach ($transition->responses as $key => $response): ?>
                                        <div class="card">
                                            <div class="card-header">
                                                <h6 class="card-title response"
                                                    data-toggle="collapse"
                                                    data-target="#request-coll--<?= $transition->get_href() . '-' . $response->statuscode; ?>">
                                                    Response <var
                                                        class="<?= $this->get_response_status($response->statuscode) ?>"><?= $response->statuscode." ".$this->http_status->getReasonPhrase($response->statuscode)?></var>
                                                    <span class="fas indicator fa-angle-<?= ($key < 1) ? 'down' : 'up'; ?> float-right"></span>
                                                </h6>
                                            </div>
                                            <div
                                                class="card-body collapse <?php if ($key < 1): ?>show<?php endif; ?> response-card"
                                                id="request-coll--<?= $transition->get_href() . '-' . $response->statuscode; ?>"
                                                data-anchor-id="request-coll--<?= $transition->get_href() . '-' . $response->statuscode; ?>">
                                                <?= $response->description; ?>
                                                <?php if ($response->headers !== []): ?>
                                                    <h5>Headers</h5>
                                                    <ul class="headers list-unstyled">
                                                        <?php foreach ($response->headers as $name => $value): ?>
                                                            <li>
                                                                <code><span class="attr"><?= $name; ?></span>: <span
                                                                        class="value"><?= $value; ?></span>
                                                                </code>
                                                            </li>
                                                        <?php endforeach; ?>
                                                    </ul>
                                                <?php endif; ?>
                                                <?php if ($response->structure !== []): ?>
                                                    <h5>Data Structure</h5>
                                                    <div class="row">
                                                        <?php foreach ($response->structure as $value): ?>
                                                            <?= $value; ?>
                                                  <?php endforeach; ?>
                                                    </div>
                                                <?php endif; ?>
                                                <?php foreach ($response->content as $response_key => $value): ?>
                                                    <div>
                                                        <?php $href =
                                                            $response->get_id() . '-' . $response->statuscode . '-' . str_replace([
                                                                '/',
                                                                '+',
                                                            ], '-', $response_key); ?>
                                                        <h5 class="response-body"
                                                            data-toggle="collapse"
                                                            data-target="#response-<?= $href ?>">
                                                            <span class="fas indicator fa-angle-up"></span>
                                                            <?= $response_key; ?>

                                                        </h5>
                                                        <pre class="collapse response-body"
                                                             id="response-<?= $href ?>"
                                                             data-anchor-id="response-<?= $href ?>"><?= $value; ?></pre>
                                                    </div>
                                                <?php endforeach; ?>
                                            </div>
                                        </div>
                                    <?php endforeach; ?>
                                <?php endif; ?>
                            </div>
                        </div>
                    <?php endforeach; ?>
                <?php endforeach; ?>
            <?php endforeach; ?>
            <?php if (count($this->base_structures) > 0): ?>
                <h2><a data-anchor-id="datastructures" id="datastructures">Data structures</a></h2>
                <?php foreach ($this->base_structures as $key => $structure): ?>
                    <div class="card object-<?= $this->strip_link_spaces($key); ?> structure">
                        <div class="card-header">
                            <h5 class="card-title">
                                <a id="object-<?= $this->strip_link_spaces($key); ?>" data-anchor-id="object-<?= $this->strip_link_spaces($key); ?>"><?= $key; ?></a>
                            </h5>
                        </div>
                        <div class="card-body">
                            <?= $structure->description; ?>
                            <div class="row">
                                <?= $structure; ?>
                            </div>
                        </div>
                    </div>
                <?php endforeach; ?>
            <?php endif; ?>
        </div>
    </div>
</div>
<?php $extras = array_filter($this->base_data, function ($value) {
    return !in_array($value, ['HOST', 'TITLE', 'ALT_HOST', 'FORMAT', 'DESC', 'COLOR_1', 'COLOR_2']);
}, ARRAY_FILTER_USE_KEY);
if ($extras !== []):
    $extras['host'] = $this->base_data['HOST']; ?>
    <button
        type="button"
        class="btn btn-lg extra-info"
        data-toggle="popover"
        title="Extra info"
        data-placement="top"
        data-html="true"
        data-content="<?php foreach ($extras as $key => $value): ?><p><?= $key; ?>: <?= $value; ?></p><?php endforeach; ?>">
        <span class="fas fa-question-circle"></span>
    </button>
<?php endif; ?>
<?php foreach ($this->js as $js): ?>
    <script src="<?= $js ?>"></script>
<?php endforeach; ?>
<script src="https://code.jquery.com/jquery-3.4.1.slim.min.js" integrity="sha384-J6qa4849blE2+poT4WnyKhv5vZF5SrPo0iEjwBvKU7imGFAV0wwj1yYfoRSJoZ+n" crossorigin="anonymous"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.1/umd/popper.min.js" integrity="sha256-/ijcOLwFf26xEYAjW75FizKVo5tnTYiQddPZoLUHHZ8=" crossorigin="anonymous"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/anchor-js/4.2.2/anchor.min.js" integrity="sha256-E4RlfxwyJVmkkk0szw7LYJxuPlp6evtPSBDlWHsYYL8=" crossorigin="anonymous"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js" integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6" crossorigin="anonymous"></script>
<script><?= Minifier::minify_js(file_get_contents($this->find_include_file($this->template, 'js'), true)); ?></script>
</body>
</html>